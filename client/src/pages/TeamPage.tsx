import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { formatDate } from "@/lib/format";
import { trpc } from "@/lib/trpc";
import { USER_ROLE_LABELS } from "@shared/expenseConstants";
import { ShieldCheck, UserCog, UsersRound } from "lucide-react";
import { toast } from "sonner";

export default function TeamPage() {
  const { user } = useAuth();
  const utils = trpc.useUtils();
  const canManage = user?.role === "admin";
  const users = trpc.projectUsers.list.useQuery(undefined, { enabled: user?.role === "contador" || user?.role === "admin" });
  const assignRole = trpc.projectUsers.assignRole.useMutation({ onSuccess: () => utils.projectUsers.list.invalidate() });
  const applyRole = async (userId: number, role: "socio_1" | "socio_2" | "participante" | "contador" | "admin" | "user") => { try { await assignRole.mutateAsync({ userId, role }); toast.success("Rol actualizado."); } catch (error) { toast.error(error instanceof Error ? error.message : "No fue posible actualizar el rol."); } };
  if (user?.role !== "contador" && user?.role !== "admin") return <div className="mx-auto max-w-2xl rounded-xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-900">Esta vista está disponible para Contador y Administrador.</div>;
  return <div className="mx-auto max-w-5xl space-y-6"><section><p className="text-xs font-semibold uppercase tracking-[0.14em] text-[#9b7135]">Gobernanza del proyecto</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-[#253142]">Equipo y roles</h1><p className="mt-2 text-sm text-slate-600">El Contador puede consultar los usuarios. El Administrador asigna roles, incluyendo participantes adicionales para cargar gastos. Cada persona debe iniciar sesión una vez para aparecer en este listado.</p></section><Card className="border-[#e7e0d5] bg-white shadow-sm"><CardHeader className="flex-row items-center gap-3 border-b border-[#eee8de] pb-4"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#eef1ff] text-[#4667d9]"><UsersRound className="h-5 w-5" /></span><CardTitle className="text-lg text-[#253142]">Usuarios con acceso</CardTitle></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-[#fbfaf8] text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Nombre operativo</th><th className="px-5 py-3">Correo</th><th className="px-5 py-3">Rol del proyecto</th><th className="px-5 py-3">Último acceso</th><th className="px-5 py-3">Administración</th></tr></thead><tbody className="divide-y divide-[#eee8de]">{users.data?.map(member => <tr key={member.id}><td className="px-5 py-4 font-medium text-[#253142]"><p>{member.displayName || member.name || "Sin nombre"}</p>{member.displayName && member.name && member.displayName !== member.name ? <p className="mt-0.5 text-xs font-normal text-slate-500">Cuenta: {member.name}</p> : null}</td><td className="px-5 py-4 text-slate-600">{member.email || "—"}</td><td className="px-5 py-4"><span className="inline-flex rounded-full bg-[#f4efe7] px-2.5 py-1 text-xs font-medium text-[#5a4b37]">{USER_ROLE_LABELS[member.role]}</span></td><td className="px-5 py-4 text-slate-600">{formatDate(member.lastSignedIn)}</td><td className="px-5 py-4">{canManage ? <select value={member.role} onChange={event => applyRole(member.id, event.target.value as "socio_1" | "socio_2" | "participante" | "contador" | "admin" | "user")} disabled={assignRole.isPending} className="h-9 rounded-lg border border-[#ddd5c9] bg-white px-2 text-xs text-[#253142] outline-none focus:border-[#d89637]"><option value="user">Usuario sin asignar</option><option value="socio_1">Socio 1 (Ing. Angomas)</option><option value="socio_2">Socio 2 (Ing. Johan)</option><option value="participante">Participante</option><option value="contador">Contador</option><option value="admin">Administrador</option></select> : <span className="inline-flex items-center gap-1 text-xs text-slate-400"><ShieldCheck className="h-3.5 w-3.5" />Solo lectura</span>}</td></tr>)}</tbody></table></div>{users.isLoading ? <div className="p-6 text-sm text-slate-500">Cargando usuarios…</div> : null}{!users.isLoading && (users.data?.length ?? 0) === 0 ? <div className="flex items-center gap-3 p-6 text-sm text-slate-500"><UserCog className="h-5 w-5" />Los usuarios aparecerán aquí cuando inicien sesión por primera vez.</div> : null}</CardContent></Card></div>;
}
